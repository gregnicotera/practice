/**
 * @param {number} x - can be large numbers
 * @return {boolean} - if x is palindrome
 */
var isPalindrome = function(x) {
     x = x.toString().split(``);
	 for(let i = 0; i < x.length / 2){
		 
	 }
};